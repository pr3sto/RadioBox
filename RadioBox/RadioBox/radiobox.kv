<PlayerScreen>:
	orientation: 'vertical'

	# list of stations
	Button:

	BoxLayout:
		size_hint: (1.0, 0.2)

		# play/stop button
		Button:
			size_hint: (None, 1.0) 
			width: self.height

		# cover image
		Button:
			size_hint: (None, 1.0) 
			width: self.height

		# title
		Button:

		# volume slider
		Button:
			size_hint: (None, 1.0) 
			width: self.height